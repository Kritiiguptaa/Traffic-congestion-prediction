.git
.gitignore
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.vscode/
.idea/
server_run.log
server_err.log
# dataset is downloaded during build, not copied
*.xlsx
*.xls
hotspots.csv

# pipeline cache is generated fresh inside the image during build, never
# copied in from the host (a stale local cache must never ship in the image)
pipeline_cache.pkl
